{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <nav class=" bg-gray-700 shadow-md">
                <div class="container mx-auto px-12 flex justify-between items-center h-16">
                    <!-- Logo Section -->
                    <a href="/" class="text-3xl font-bold text-blue-600">
                    Artify
                    </a>

                    <!-- Buttons Section -->
                    <div class="space-x-4">
                        <a href="/login" class="px-4 py-2 border border-blue-600 text-blue-600 rounded hover:bg-blue-600 hover:text-white transition duration-300">
                            Log In
                        </a>
                        <a href="/register" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition duration-300">
                            Sign In
                        </a>
                    </div>
                </div>
    </nav>
{% endblock %}
